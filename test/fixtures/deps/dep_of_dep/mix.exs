defmodule DepOfDep.MixProject do
  use Mix.Project

  def project do
    [
      app: :dep_of_dep,
      version: "0.0.1",
      package: package()
    ]
  end

  defp package do
    [
    ]
  end
end
